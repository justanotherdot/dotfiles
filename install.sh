#!/bin/bash

# This is a stub;
# The idea is to match the *nix OS and install the apropriate sub dir.
# that way we don't need to force clone anything anymore.
# One can just pull the directory someplace and run this script.
